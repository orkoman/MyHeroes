using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using TestGameLogic.units;

namespace TestGameLogic.main.Requirements
{
    /*public class PartRequirement : baseUnitRequirement
    {
        Type partType = null;

        public PartRequirement(Type partType)
        {
            this.partType = partType;
        }

        protected override bool check(baseUnit unit)
        {
            return (unit.countFreeParts(partType) >= 1);
        }
    }*/
}
